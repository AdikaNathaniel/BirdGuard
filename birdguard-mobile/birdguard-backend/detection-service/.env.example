# detection-service environment configuration - copy to .env and adjust as needed
DETECTION_SERVICE_HOST=127.0.0.1
DETECTION_SERVICE_PORT=3003

# MongoDB connection string (same database auth-service uses)
MONGODB_URI=mongodb://localhost:27017/birdguard
